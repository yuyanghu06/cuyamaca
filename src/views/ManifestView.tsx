export default function ManifestView() {
  return (
    <div className="view-placeholder">
      <div className="view-placeholder-title">Manifest</div>
      <div className="view-placeholder-subtitle">
        Define your hardware — board type, components, pin assignments. The manifest is the model's
        ground truth for code generation.
      </div>
    </div>
  );
}
